// import React, { useState } from "react";

// const complex = (num) => {
//   let i = 0
// }

// export const Counter = () => {
//   const [num, setNum] = useState(0);

//   const computed = complex(num)

//   return (
//     <>
//       <h1>{num}</h1>
//       <button className="mbtn" onClick={() => setNum(prev => prev - 1)}>-</button>
//       <button className="pbtn" onClick={() => setNum(prev => prev + 1)}>+</button>
//     </>
//   );
// };
